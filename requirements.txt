# pos-siesa-remake — dependencias mínimas (Ponytail: solo lo necesario)
# La lógica de negocio usa la stdlib (sqlite3 incluido). Aquí solo lo que la stdlib no cubre.

PySide6>=6.6        # interfaz de caja (Qt6)
pyserial>=3.5       # balanza serial (fruver) — adaptador BalanzaSerial

# Desarrollo / pruebas
pytest>=8.0
